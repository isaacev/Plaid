package types

// Int is the canonical integer type symbol
var Int = TypeIdent{"Int"}

// Str is the canonical string type symbol
var Str = TypeIdent{"Str"}

// Bool is the canonical boolean type symbol
var Bool = TypeIdent{"Bool"}
